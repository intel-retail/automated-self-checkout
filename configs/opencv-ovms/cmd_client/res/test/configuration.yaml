OvmsClient:
  PipelineScript: run_test.sh
  PipelineInputArgs: "" # space delimited like we run the script in command and take those input arguments
  PipelineStreamDensityRun: ""
  EnvironmentVariableFiles:
    - object_detection.env
